# Vite client configuration
VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace_me
VITE_CONVEX_URL=https://replace-me.convex.cloud

# Convex dashboard environment variable
CLERK_JWT_ISSUER_DOMAIN=https://replace-me.clerk.accounts.dev

# Convex dashboard environment variables; never expose these to Vite/browser code
YOUTUBE_API_KEY=replace_with_restricted_youtube_data_api_key
TVNERD_ALLOWED_ORIGINS=https://your-site.netlify.app

# Keep this server-side only when a backend server is added
CLERK_SECRET_KEY=sk_test_replace_me
